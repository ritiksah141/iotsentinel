# Core Dependencies
# Pins carry cp311 + cp312 wheels (3.12 support: numpy>=1.26, pandas>=2.1, scikit-learn>=1.3.2).
# numpy stays <2 so scikit-learn 1.3.x and River 0.21 keep their ABI.
numpy==1.26.4
pandas==2.3.2
scikit-learn==1.3.2

# Machine Learning - River for incremental learning (Pi-optimized)
river==0.21.0  # Lightweight ML for anomaly detection

# AI Assistant - Hybrid cloud/local LLM (optional)
groq>=0.13.0  # Free cloud API (14,400 requests/day); older pins break with httpx>=0.28
openai>=1.0.0  # Optional: OpenAI cloud API (business tier)
anthropic>=0.40.0  # Optional: Claude cloud API (paid)

# Network Analysis - Using scapy instead of pypcap (easier to install)
scapy==2.5.0

# Web Dashboard
dash==2.14.0
dash-bootstrap-components==1.5.0
dash-cytoscape==0.3.0
plotly==5.18.0
Flask-Compress==1.14  # Gzip compression for better performance

# Database
# sqlite3 is built into Python

# Authentication & Security
Authlib==1.3.0                 # OAuth 2.0 client
webauthn==1.11.1               # WebAuthn/FIDO2 support
zxcvbn==4.4.28                  # Password strength meter
email-validator==2.1.0          # Email validation
cryptography==41.0.7            # Required by WebAuthn
bcrypt==4.1.2                   # Password hashing (if not already installed)
Flask-Login==0.6.3              # Session management
pyotp==2.9.0                    # TOTP/2FA token generation
qrcode[pil]==8.2                # QR code generation for 2FA

# Utilities
python-dateutil==2.8.2
requests==2.31.0
paramiko==3.4.0
manuf==1.1.5

# Development
pytest==7.4.3
pytest-cov==4.1.0
psutil==5.9.6
black==23.12.0
pylint==3.0.3
ipython==8.18.1
python-dotenv
apscheduler
Flask-SocketIO
flask-sock
eventlet
dash-extensions

# Export & Reporting (Added 2026-01-04)
reportlab==4.0.7          # PDF generation
openpyxl==3.1.2           # Excel file generation
Pillow==10.1.0            # Image processing for PDF charts

# IoT Discovery & Vulnerability Management (Added 2026-01-06)
zeroconf>=0.112.0         # mDNS/Zeroconf service discovery
python-nmap>=0.7.1        # nmap integration for network scanning
fuzzywuzzy>=0.18.0        # Fuzzy string matching for CVE matching
python-Levenshtein>=0.21.0  # Fast fuzzy matching backend
